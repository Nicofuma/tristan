<div class="details">
    <span class="filter">
        <a href="{{ path('homepage', app.request.query.all|merge({'country': job.company.country})) }}">{{ job.company.city }}, {{ job.company.country|country }}</a> /
        <a href="{{ path('homepage', app.request.query.all|merge({'country': job.company.country, 'contractType': job.contractType})) }}">{{ job.contractType|contractType }}</a>

        {#<br/><span class="archived">Archived since 12/12/2013</span>#}

    </span>
</div>

<a href="{{ path('job_show', {'country': job.company.country , 'contract': job.contractType, 'slug': job.slug }) }}" class="title">
    {{ job.title }}
    <span class="company">@&nbsp;{{ job.company.name }}</span>
</a>

<a href="{{ path('job_show', {'country': job.company.country , 'contract': job.contractType, 'slug': job.slug }) }}">
    <span class="cover"></span>
</a>
